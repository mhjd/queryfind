# Basic Synthetic Corpus

This corpus is intentionally small and human-readable.

It contains:

- client contracts and notes
- operations runbooks and incident notes
- project briefs and status updates
- onboarding material
- a hidden alias note

The first-pass evaluation cases are defined in `queryfind/synthetic_eval.py`.
