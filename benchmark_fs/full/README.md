# Full Benchmark Corpus

This corpus is larger and more ambiguous than the synthetic smoke corpus.

It is intended to compare models on:

- path search
- content search
- metadata-aware queries like `latest`
- hidden-file discovery
- evidence retrieval through snippets
- a small amount of alias-based reasoning

The executable case definitions live in `benchmark_fs/full_manifest.json`.
